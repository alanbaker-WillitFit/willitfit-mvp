(() => {
  "use strict";

  // Terminal Chase character derivative contract.
  // Approved source sheets remain authoritative; only transparent, game-specific
  // derivatives should be referenced here once they have been visually verified.
  window.WILLIT_TERMINAL_CHASE_CHARACTERS = Object.freeze({
    player: null,
    hazards: Object.freeze({
      staff: null,
      cart: null,
      traveller: null,
      security: null,
    }),
  });
})();
